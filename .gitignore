.omo/
.idea/
.ocprofile
.DS_Store

# node / yarn (berry, node-modules linker)
node_modules/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# vitepress build output
docs/.vitepress/dist/
docs/.vitepress/cache/

# git-cliff generated notes (CI only)
RELEASE_NOTES.md

# generated binary (built by bashly; released via CI, not committed)
/ocp
